import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    image:{
        height: "60%",
        width: "50%",
        resizeMode: "cover",
    },
    subtitle: {
        fontSize: 20,
        color: "#ABABAB",
    }
});